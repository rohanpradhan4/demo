class Solution {
    public boolean containsDuplicate(int[] nums) {
        
        HashSet<Integer> list = new HashSet<>();
        
        for(int n : nums){
            if(!list.add(n)){
                return true;
            }
        }
        
        return false;
    
    }
}
